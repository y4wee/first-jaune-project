import { Ifollower } from "../../interfaces/network";

const Followers = ({ followers }: { followers: Ifollower[] }) => {
  // console.log(followers);
  return (
    <div>
      <h1>Followers</h1>
    </div>
  );
};

export default Followers;
