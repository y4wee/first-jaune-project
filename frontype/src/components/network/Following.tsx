import { Ifollowing } from "../../interfaces/network";

const Following = ({ following }: { following: Ifollowing[] }) => {
  return (
    <div>
      <h1>Following</h1>
    </div>
  );
};

export default Following;
